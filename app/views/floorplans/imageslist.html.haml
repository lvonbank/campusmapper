%h1{:class => "floorPlan"} Image Floor Plans

%table{:class => "floorPlanTable"}
    %thead
        %tr
            %th Image Name
            %th
            %th 
            %th 
    %tbody
        - @floor_plans.each do |plan|
            %tr
                %td= plan.name
                %td= link_to "Details", image_path(plan.imageSource)
                %td= link_to "Edit"
                %td= link_to "Delete"
                

%btn{:class => "addBtn"}Add new image